# earnings-calls-stock-predictor
ML aplicado a earnings calls de empresas tech: combina NLP con FinBERT, EPS surprise y momentum para detectar movimientos post-anuncio. El modelo duplicó el win rate del baseline usando un ensemble XGBoost + LightGBM con 228 predicciones out-of-sample.
